{
  "name": "obrareport-site",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5500",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "cd backend && node --test tests/*.test.js",
    "test:e2e": "playwright test",
    "build:public-site": "npm run build"
  },
  "devDependencies": {
    "@playwright/test": "^1.45.0",
    "vite": "^5.4.11"
  }
}
